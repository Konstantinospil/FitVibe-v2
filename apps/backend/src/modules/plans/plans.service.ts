/**
 * Placeholder implementation until full plans module lands.
 * Sessions module depends on `recomputeProgress` to refresh plan aggregates.
 */
export async function recomputeProgress(_userId: string, _planId: string): Promise<void> {
  // Intentionally left blank; real implementation will recalculate plan metrics.
}
